extend layout

block content
  h1 Create New Class

  .row
    .span6
      form(action="/coursesclasses", method="POST")
        label(for="name") Name
        input(type="text", name="name")

        label(for="description") Description
        input(type="text", name="description")

        input.btn.btn-primary.pull-right(type="submit", value="Create &raquo;")
